# Agentic Kubernetes Platform — secrets (copy to .env and fill in)
# .env is git-ignored; agents inside DevContainers cannot read these files.
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
GRAFANA_SERVICE_ACCOUNT_TOKEN=
